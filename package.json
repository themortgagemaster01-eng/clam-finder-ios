{
  "name": "clam-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Clam Finder — native iOS shell (Capacitor) for the clamming & fishing PWA",
  "scripts": {
    "sync": "cap sync",
    "ios:add": "cap add ios",
    "ios:sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
